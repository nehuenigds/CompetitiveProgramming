#include<bits/stdc++.h>

using namespace std;

int analizar(vector<vector<int>> muestras){
    int n=muestras.size();
    for (int i=0; i<muestras.size(); i++){
        int m=n
        int ta=0, ti=0;
        for(j=0; j<muestras[i].size();j++){
            if(i<muestras[i][j]){
                if(muestras[i][j]-i<m){
                    m=muestras[i][j];
                }
            }
        
        }
    }
}

int main()
{
    
}