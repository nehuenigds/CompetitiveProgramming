/*
ordenar dos palabras, infresadas por input del usuario, 
by: Ian Joa Neu

pasos

decirle al programa como estan ordenadas las letras
[a, b, c, d, e ...., z]


agarra la letra numero i de cada palabra
    ver en que pociocion esta cada uno
    iguales 
        ver el siguiente caracter
        volvemos a intentar
    dintinatas posiciones
        si la primera pal}
        salimos

ver en que pocicion estan
*/

#include <iostream>
#include <string>
 
using namespace std;

int main()
{
    //definimos el orden del abecedario


    string palabra1, palabra2;
    bool condicion = true
    char char1
    char char2
    cout << "Ingrese dos palabras para ver su orden en el abecedario"; cin >> palabra1, palabra2;
    
    
        while(condicion)    //bucle hasta encontrar la diferencia, por cada caracter
    {
        i = 1;
        char1 = palabra1[i]
        char2 = palabra1[i]
        /*si en encuentro la diferencia
            salimos, y se indiaca al usuario (condicion falsa)
        */
    }    

    return 0;
}
